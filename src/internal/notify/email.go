package notify

import conf "github.com/0xNF/glm/src/internal/conf"

// SendEmail uses the specified Email settings to end an email to the Monitoring administrator
func SendEmail(emailConf *conf.GLMEmail) {
	return
}
